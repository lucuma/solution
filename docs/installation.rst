============
Installation
============

At the command line::

    $ easy_install pip
    $ pip install solution

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv solution
    $ pip install solution