# This file collects all required third-party libraries that are needed
# to run this project. Later you can install all of them in a row
# using pip. Example:
#
#     pip install -r requirements.txt

markupsafe
